name: "dbt_workshop"
version: "1.0.0"
config-version: 2

profile: "dbt_workshop"

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

models:
  dbt_workshop:
    +materialized: view


