name: "dbt_workshop"
version: "1.0.0"
config-version: 2

profile: "dbt_workshop"

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

models:
  dbt_workshop:
    +materialized: view

vars:
  lookback_days: [3, 7, 14]
  max_feature_date: '2018-10-31'
  churn_threshold_days: 90
  min_order_value: 10
